services:
  happytorch:
    image: ghcr.io/chan/happytorch:latest
    build: .
    ports:
      - "8000:8000"
    environment:
      - MODE=web
      - HOST=0.0.0.0
    volumes:
      - ./data:/app/data
    restart: unless-stopped
